# @lithekit/date

Small native `Date`, `Intl`, and Temporal-aware utilities.

```ts
import { addDays } from "@lithekit/date"

const tomorrow = addDays(new Date(), 1)
```

See the [Lithekit documentation](https://sousaivan99.github.io/lithekit/modules/date/).
