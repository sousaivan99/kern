// Generated by the Kern model compiler experiment. Do not edit.
import {
  COMPILED_FAILURE,
  fallbackFast,
  isEmail,
  isPlainRecord,
  isUrl,
  isUuid,
} from "../runtime.js"

const AdvancedUserFast = (input) => {
  try {
    const objectValue0 = input
    if (!isPlainRecord(objectValue0)) return COMPILED_FAILURE
    const objectOutput1 = {}
    const hasProperty2 = Object.hasOwn(objectValue0, "id")
    if (!hasProperty2) return COMPILED_FAILURE
    let stringValue3 = hasProperty2 ? objectValue0["id"] : undefined
    if (typeof stringValue3 !== "string") return COMPILED_FAILURE
    if (!isUuid(stringValue3)) return COMPILED_FAILURE
    objectOutput1["id"] = stringValue3
    const hasProperty4 = Object.hasOwn(objectValue0, "profile")
    if (!hasProperty4) return COMPILED_FAILURE
    const objectValue5 = hasProperty4 ? objectValue0["profile"] : undefined
    if (!isPlainRecord(objectValue5)) return COMPILED_FAILURE
    const objectOutput6 = {}
    const hasProperty7 = Object.hasOwn(objectValue5, "displayName")
    if (!hasProperty7) return COMPILED_FAILURE
    let stringValue8 = hasProperty7 ? objectValue5["displayName"] : undefined
    if (typeof stringValue8 !== "string") return COMPILED_FAILURE
    stringValue8 = stringValue8.trim()
    if (stringValue8.length < 2) return COMPILED_FAILURE
    if (stringValue8.length > 80) return COMPILED_FAILURE
    objectOutput6["displayName"] = stringValue8
    const hasProperty9 = Object.hasOwn(objectValue5, "biography")
    const optionalInput10 = hasProperty9 ? objectValue5["biography"] : undefined
    let optionalOutput11
    if (optionalInput10 === undefined) {
      optionalOutput11 = undefined
    } else {
      let stringValue12 = optionalInput10
      if (typeof stringValue12 !== "string") return COMPILED_FAILURE
      stringValue12 = stringValue12.trim()
      if (stringValue12.length > 500) return COMPILED_FAILURE
      optionalOutput11 = stringValue12
    }
    if (hasProperty9 && optionalOutput11 !== undefined) objectOutput6["biography"] = optionalOutput11
    const hasProperty13 = Object.hasOwn(objectValue5, "address")
    if (!hasProperty13) return COMPILED_FAILURE
    const objectValue14 = hasProperty13 ? objectValue5["address"] : undefined
    if (!isPlainRecord(objectValue14)) return COMPILED_FAILURE
    const objectOutput15 = {}
    const hasProperty16 = Object.hasOwn(objectValue14, "street")
    if (!hasProperty16) return COMPILED_FAILURE
    let stringValue17 = hasProperty16 ? objectValue14["street"] : undefined
    if (typeof stringValue17 !== "string") return COMPILED_FAILURE
    stringValue17 = stringValue17.trim()
    if (stringValue17.length < 1) return COMPILED_FAILURE
    objectOutput15["street"] = stringValue17
    const hasProperty18 = Object.hasOwn(objectValue14, "city")
    if (!hasProperty18) return COMPILED_FAILURE
    let stringValue19 = hasProperty18 ? objectValue14["city"] : undefined
    if (typeof stringValue19 !== "string") return COMPILED_FAILURE
    stringValue19 = stringValue19.trim()
    if (stringValue19.length < 2) return COMPILED_FAILURE
    objectOutput15["city"] = stringValue19
    const hasProperty20 = Object.hasOwn(objectValue14, "countryCode")
    if (!hasProperty20) return COMPILED_FAILURE
    let stringValue21 = hasProperty20 ? objectValue14["countryCode"] : undefined
    if (typeof stringValue21 !== "string") return COMPILED_FAILURE
    stringValue21 = stringValue21.trim()
    if (stringValue21.length !== 2) return COMPILED_FAILURE
    objectOutput15["countryCode"] = stringValue21
    const hasProperty22 = Object.hasOwn(objectValue14, "postalCode")
    if (!hasProperty22) return COMPILED_FAILURE
    let stringValue23 = hasProperty22 ? objectValue14["postalCode"] : undefined
    if (typeof stringValue23 !== "string") return COMPILED_FAILURE
    stringValue23 = stringValue23.trim()
    if (stringValue23.length < 3) return COMPILED_FAILURE
    if (stringValue23.length > 12) return COMPILED_FAILURE
    objectOutput15["postalCode"] = stringValue23
    objectOutput6["address"] = objectOutput15
    objectOutput1["profile"] = objectOutput6
    const hasProperty24 = Object.hasOwn(objectValue0, "roles")
    if (!hasProperty24) return COMPILED_FAILURE
    const arrayValue25 = hasProperty24 ? objectValue0["roles"] : undefined
    if (!Array.isArray(arrayValue25)) return COMPILED_FAILURE
    if (arrayValue25.length < 1) return COMPILED_FAILURE
    if (arrayValue25.length > 10) return COMPILED_FAILURE
    const arrayOutput26 = new Array(arrayValue25.length)
    for (let index27 = 0; index27 < arrayValue25.length; index27 += 1) {
      let stringValue28 = arrayValue25[index27]
      if (typeof stringValue28 !== "string") return COMPILED_FAILURE
      stringValue28 = stringValue28.trim()
      if (stringValue28.length < 1) return COMPILED_FAILURE
      arrayOutput26[index27] = stringValue28
    }
    objectOutput1["roles"] = arrayOutput26
    const hasProperty29 = Object.hasOwn(objectValue0, "preferences")
    if (!hasProperty29) return COMPILED_FAILURE
    const objectValue30 = hasProperty29 ? objectValue0["preferences"] : undefined
    if (!isPlainRecord(objectValue30)) return COMPILED_FAILURE
    const objectOutput31 = {}
    const hasProperty32 = Object.hasOwn(objectValue30, "marketingEmails")
    const defaultInput33 = hasProperty32 ? objectValue30["marketingEmails"] : undefined
    let defaultOutput34
    if (defaultInput33 === undefined) {
      defaultOutput34 = false
    } else {
      const booleanValue35 = defaultInput33
      if (typeof booleanValue35 !== "boolean") return COMPILED_FAILURE
      defaultOutput34 = booleanValue35
    }
    objectOutput31["marketingEmails"] = defaultOutput34
    const hasProperty36 = Object.hasOwn(objectValue30, "locale")
    const defaultInput37 = hasProperty36 ? objectValue30["locale"] : undefined
    let defaultOutput38
    if (defaultInput37 === undefined) {
      defaultOutput38 = "en"
    } else {
      let stringValue39 = defaultInput37
      if (typeof stringValue39 !== "string") return COMPILED_FAILURE
      stringValue39 = stringValue39.trim()
      if (stringValue39.length < 2) return COMPILED_FAILURE
      if (stringValue39.length > 10) return COMPILED_FAILURE
      defaultOutput38 = stringValue39
    }
    objectOutput31["locale"] = defaultOutput38
    objectOutput1["preferences"] = objectOutput31
    const hasProperty40 = Object.hasOwn(objectValue0, "score")
    if (!hasProperty40) return COMPILED_FAILURE
    const numberValue41 = hasProperty40 ? objectValue0["score"] : undefined
    if (typeof numberValue41 !== "number" || Number.isNaN(numberValue41)) return COMPILED_FAILURE
    if (!Number.isFinite(numberValue41)) return COMPILED_FAILURE
    if (numberValue41 < 0) return COMPILED_FAILURE
    if (numberValue41 > 100) return COMPILED_FAILURE
    objectOutput1["score"] = numberValue41
    return objectOutput1
  } catch {
    return COMPILED_FAILURE
  }
}

export { AdvancedUserFast }
